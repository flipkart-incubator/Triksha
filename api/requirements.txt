# Triksha API Dependencies - Matching Local Working Environment

# Web framework (exact versions from local pip list)
fastapi==0.123.5
uvicorn[standard]==0.37.0
starlette==0.50.0

# Authentication and security (exact versions from local pip list)
PyJWT==2.10.1
cryptography>=44.0.0
bcrypt>=4.0.1
python-multipart==0.0.20

# Data validation (exact versions from local pip list)
pydantic==2.12.4
pydantic-settings==2.12.0
email-validator==2.1.0

# HTTP client for model APIs (exact versions from local pip list)
httpx==0.28.1
httpx-sse==0.4.0
aiohttp>=3.11.0

# Database
# SQLite is built into Python for local development
# PostgreSQL via psycopg2 (see pg_database.py) when DATABASE_URL is set

# Utilities (exact versions from local pip list)
python-dotenv==1.2.1
rich==13.7.0

# Background tasks
asyncio==4.0.0

# Kafka messaging (distributed scan queue).
# Only required when TRIKSHA_USE_KAFKA=true; safe to install always.
aiokafka>=0.10.0

# Documentation
swagger-ui-bundle==0.0.9

# Additional dependencies for standalone functionality (exact versions from local pip list)
requests==2.32.5
pandas>=2.2.2
openpyxl>=3.1.0
numpy>=1.26.4

# Machine Learning for dataset poisoning detection (exact versions from local pip list)
scikit-learn>=1.4.0

# MCP Scanner dependencies (exact versions from local pip list)
pyjson5==2.0.0
mcp==1.16.0
sse-starlette==3.0.2
jsonschema==4.23.0

# PDF Report Generation
reportlab==4.2.5

# Visual prompt injection attacks (image generation)
Pillow>=10.0.0

# Google ADK (Agent Development Kit) for agent security scanner
google-adk>=1.0.0
google-genai>=1.0.0

# GCP authentication (WIF / service account impersonation)
google-auth>=2.29.0
python-docx>=1.1.0
mammoth>=1.6.0

# AI Asset Discovery
SQLAlchemy>=2.0.0
PyYAML>=6.0

# Triksha: direct LLM provider SDKs
openai>=1.40.0
anthropic>=0.34.0

# Triksha: PostgreSQL handler + pgvector semantic store (recommended default)
psycopg2-binary>=2.9.9
# Async drivers for ADK's DatabaseSessionService (durable copilot memory)
asyncpg>=0.29.0
aiosqlite>=0.19.0
